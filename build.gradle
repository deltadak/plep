description = """ 
Gradle build file originally written to let Travis access dependencies and build. 

I had to set gradle home to C:\\Users\\s156757\\.gradle\\wrapper\\dists\\gradle-3.3-all\\55gk...\\gradle-3.3 in Settings - Build tools - Gradle and add the same with \\bin added to my path (Travis will use it's own gradle), similarly for jdk home\\lib """

apply plugin: 'java'

test {
//    include "**/ControllerTest.class"
    dependsOn 'cleanTest' // run tests every time

}

tasks.withType(Test) {
    scanForTestClasses = false
    include "**/*Test.class"
}

sourceSets {
    main {
        java {
            srcDirs 'src/deltadak'
        }
    }
    test {
        java {
            srcDirs 'test/deltadak'
        }
    }
}

//task testgradle {
//    doLast {
//        println 'Hello Gradle'
//    }
//}

repositories {
    mavenCentral()
//    flatDir {
//        dirs 'lib'
//    }
}

dependencies {

    //1.
    testCompile("org.junit.jupiter:junit-jupiter-api:5.0.0-M3")
    //6.
    testCompile 'org.hamcrest:hamcrest-library:1.3'

//    compile name: 'hamcrest-all-1.3'
//    compile name: 'hamcrest-core-1.3'
//    compile name: 'hamcrest-library-1.3'
//    compile name: 'junit'
//    compile name: 'junit-jupiter-api-5.0.0-M4'
//    compile name:
//    compile name: 'junit.jar'


    // tries below to add from local lib didn't work
//    compile fileTree(dir: 'lib', include: ['**/*.jar'])
//    compile files('*.jar')
//    compile name: 'lib/*.jar'
//    compile name: '**'
    // both tries below to download from maven repository didn't find hamcrest

//    testCompile 'org.hamcrest:hamcrest-library:1.3'
    // https://mvnrepository.com/artifact/org.hamcrest/hamcrest-all
//    compile group: 'org.hamcrest', name: 'hamcrest-all', version: '1.3'
}

test {
    testLogging {
        events "passed", "skipped", "failed"
    }
}